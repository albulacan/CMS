import { BsDatepickerDirective } from './bs-datepicker.directive';

describe('BsDatepickerDirective', () => {
  it('should create an instance', () => {
    const directive = new BsDatepickerDirective();
    expect(directive).toBeTruthy();
  });
});
